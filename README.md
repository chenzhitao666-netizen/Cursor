# 点球大战（守门员 + 三人轮番）

一个纯前端（无依赖）的小型点球游戏：**1 个守门员、3 个球员轮流射门**，默认 5 轮，共 15 次射门。

## 运行

- 方式 1（最简单）：直接双击打开 `index.html`
- 方式 2（本地服务器）：

```bash
python3 -m http.server 5173
```

然后在浏览器打开 `http://localhost:5173`

## 操作

- 选择射门方向（九宫格）
- 调整力度、弧度
- 点击“射门”
- 键盘快捷键：
  - `Enter` 或空格：射门
  - `R`：重开

